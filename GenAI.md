# 奶茶了么网页

## 主要功能

- 记录用户奶茶订单信息
- 订单信息包括奶茶品牌、产品名称、糖度、价格、购买渠道等数据
- 维护奶茶品牌列表，用户可以添加、删除、修改品牌信息，包括品牌LOGO和品牌名称
- 以日历形式展示用户奶茶订单，将奶茶品牌LOGO放置在对应日期未知，点击对应日期查看奶茶订单详细信息
- 提供订单查询功能，用户可以根据日期、品牌、产品名称等条件查询订单
- 在订单查询界面中可以选择查询出的条目进行修改
- 适配手机端（竖屏页面），提供良好的用户体验

## 技术实现

- 全栈框架: Nuxt.js
- 前端组件库: Nuxt UI
- 数据库: mysql

## 用户功能

- 在Order表新增UserId字段，用于关联用户，Brand表不变，为所有用户共享
- 新增User表，用于存储用户信息，包括用户ID、用户名、密码
- 用户注册/登录功能，用户需要输入用户名和密码进行注册/登录，需要复用用户名密码组件
- 密码在前端进行md5+salt加密，确保用户密码的安全性
- 初始启动时先检查cookie中是否有token，如果没有则直接弹出登录模态框
- 用户登陆后，服务端会返回token，并在服务端内存中维护token->UserId的映射关系
- 后续用户请求中，前端需要在cookie中携带token，服务端会验证token的有效性，若有效则继续处理请求，否则返回前端清除cookie并刷新页面
- 服务端在处理用户请求时，会根据token从内存中获取对应的UserId，将UserId添加到请求参数中，用于关联用户订单
- 在页面合适位置设置推出按钮，功能为清除cookie并刷新页面
